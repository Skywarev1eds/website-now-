<section id="{{ componentId }}" class="component" data-component-id="{{ componentId }}">
  <div class="container mx-auto px-4 mb-16 lg:mb-32">
    {% if properties.headline or properties.title or properties.subtitle %}
    <div class="flex flex-col gap-4 mb-8 text-center">
      {% if properties.headline %}
      <div>
        <span class="inline-block px-6 py-2 rounded-full border-2 border-white/5 bg-white/5 text-accent-100 text-sm font-semibold hover:translate-y-[-2px] transition-transform duration-300 ease-in-out" data-dsr>
          {{ properties.headline | renderString }}
        </span>
      </div>
      {% endif %}
      {% if properties.title %}
      <h2 class="text-5xl !leading-[1.125] font-bold text-t-primary" data-dsr>
        {{ properties.title | renderString }}
      </h2>
      {% endif %}
      {% if properties.subtitle %}
      <p class="text-base text-t-primary/50" data-dsr>
        {{ properties.subtitle | renderString }}
      </p>
      {% endif %}
    </div>
    {% endif %}
    <div class="p-8 rounded-xl border-2 border-white/5 bg-black/5 backdrop-blur-2xl overflow-hidden relative lg:p-16" data-dsr>
      <div class="mx-auto absolute -z-10 aspect-square rounded-full translate-y-[95%] left-0 right-0 bottom-0 blur-3xl bg-accent-500/40 w-[25%]" aria-hidden="true"></div>
      <div class="grid grid-cols-1 gap-16 md:grid-cols-3 md:gap-8">
        {% for stat in properties.stats %}
        <div class="flex flex-col items-center text-center">
          <p class="text-5xl font-semibold text-t-primary mb-2">
            {% if ((stat.value | renderString) | isNumber) and properties.animated and not isBuilder %}
              <span class="animate-number-counter" data-value="{{ stat.value | renderString }}">&nbsp;</span>
            {% else %}
              {{ stat.value | renderString }}
            {% endif %}
          </p>
          <h4 class="text-t-primary/50">
            {{ stat.title | renderString }}
          </h4>
        </div>
        {% endfor %}
      </div>
    </div>
  </div>
</section>